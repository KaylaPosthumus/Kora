{
  "name": "kora-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for firestore.rules — the only access control Kora has",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "@types/node": "^22.10.0"
  }
}
